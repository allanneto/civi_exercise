import React from 'react';

import * as Styled from './styles';

const Header: React.FC = () => {
  return (
    <Styled.Container>
      <Styled.Title>Civi App - Test</Styled.Title>
    </Styled.Container>
  );
};

export default Header;
